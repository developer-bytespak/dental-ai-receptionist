{
  "name": "04 - Book appointment",
  "nodes": [
    {
      "parameters": {
        "inputSource": "passthrough"
      },
      "id": "021a8a2e-1b98-5194-a4bc-0fcbb9389774",
      "name": "Sub-workflow trigger",
      "type": "n8n-nodes-base.executeWorkflowTrigger",
      "typeVersion": 1.1,
      "position": [
        0,
        300
      ]
    },
    {
      "parameters": {
        "jsCode": "// Any PMS/HTTP error lands here. Store codes only (no free text, no PHI) and hand off to workflow 12.\nconst input = $('Sub-workflow trigger').first().json;\nconst args = input.args || {};\nconst err = $json.error || {};\nconst http_status = (err && (err.httpCode || err.status || err.statusCode)) || null;\nreturn [{ json: {\n  call_id: input.call_id,\n  patient_id: args.patient_id || null,\n  location: input.location,\n  reason: 'pms_error',\n  requested: { fn: 'book_appointment', slot_id: args.slot_id || null, appointment_type: args.appointment_type || null, http_status: http_status ? String(http_status) : null },\n} }];\n"
      },
      "id": "1112431b-64bc-54ca-adf0-3887b5c97466",
      "name": "Prepare queue item",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1320,
        640
      ]
    },
    {
      "parameters": {
        "source": "database",
        "workflowId": {
          "__rl": true,
          "mode": "id",
          "value": "dental12queuenot",
          "cachedResultName": "12 - Booking queue notify"
        },
        "mode": "once",
        "options": {
          "waitForSubWorkflow": true
        }
      },
      "id": "93087fe6-b0a7-58da-abc1-b2e9c85cf4ac",
      "name": "Run 12 booking queue",
      "type": "n8n-nodes-base.executeWorkflow",
      "typeVersion": 1.1,
      "position": [
        1540,
        640
      ],
      "notes": "Target workflow id is the id embedded in the exported JSON. If your n8n instance assigned a different id on import, re-select the sub-workflow here.",
      "notesInFlow": false
    },
    {
      "parameters": {
        "jsCode": "return [{ json: { status: 'queued', queue_ref: $json.queue_ref || null, say: \"I've sent that to our team and they'll confirm with you by text or call within the hour.\" } }];"
      },
      "id": "bf723f4d-d575-5ba6-93d2-78b0aad05da2",
      "name": "Queued result",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1760,
        640
      ]
    },
    {
      "parameters": {
        "jsCode": "// Returns a cached NexHealth bearer token when still valid (cached 50 min in workflow static data).\n// Static data only persists for production (activated) executions; manual test runs re-authenticate.\nconst sd = $getWorkflowStaticData('global');\nconst valid = sd.nexhealthToken && sd.nexhealthTokenExpires && Number(sd.nexhealthTokenExpires) > Date.now();\nreturn [{ json: { token: valid ? sd.nexhealthToken : null } }];\n"
      },
      "id": "8ac39205-a65e-5f2a-a0ac-420e4b20826f",
      "name": "Token cache",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        220,
        300
      ]
    },
    {
      "parameters": {
        "conditions": {
          "options": {
            "caseSensitive": true,
            "leftValue": "",
            "typeValidation": "loose"
          },
          "conditions": [
            {
              "id": "42d75cdf-ff52-5ed7-a061-f923d1761f7e",
              "leftValue": "={{ !!$json.token }}",
              "rightValue": "",
              "operator": {
                "type": "boolean",
                "operation": "true",
                "singleValue": true
              }
            }
          ],
          "combinator": "and"
        },
        "options": {}
      },
      "id": "da08077e-c44f-5ec4-802d-88e2a064cb39",
      "name": "Has cached token?",
      "type": "n8n-nodes-base.if",
      "typeVersion": 2,
      "position": [
        440,
        300
      ]
    },
    {
      "parameters": {
        "method": "POST",
        "url": "https://nexhealth.info/authenticates",
        "options": {
          "timeout": 15000
        },
        "authentication": "genericCredentialType",
        "genericAuthType": "httpHeaderAuth",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            {
              "name": "Accept",
              "value": "application/vnd.Nexhealth+json;version=2"
            },
            {
              "name": "Nex-Api-Version",
              "value": "v3.0.0"
            }
          ]
        }
      },
      "id": "08573217-aa1c-545f-9271-bfa2e37c0b12",
      "name": "NexHealth: authenticate",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        660,
        500
      ],
      "credentials": {
        "httpHeaderAuth": {
          "name": "NexHealth API"
        }
      },
      "onError": "continueErrorOutput",
      "notes": "Header Auth credential 'NexHealth API' supplies Authorization: <NexHealth API key>. Response data.token is a JWT used as Bearer on every other call.",
      "notesInFlow": true
    },
    {
      "parameters": {
        "jsCode": "// POST https://nexhealth.info/authenticates responds { code, data: { token } }.\nconst sd = $getWorkflowStaticData('global');\nconst token = $json.data && $json.data.token;\nif (!token) { throw new Error('NexHealth /authenticates returned no token'); }\nsd.nexhealthToken = token;\nsd.nexhealthTokenExpires = Date.now() + 50 * 60 * 1000;\nreturn [{ json: { token } }];\n"
      },
      "id": "22b7f708-f073-59e1-92f8-174082d49545",
      "name": "Cache token",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        880,
        500
      ]
    },
    {
      "parameters": {
        "jsCode": "// ---- shared helpers (duplicated in each workflow on purpose: n8n Code nodes cannot import) ----\nconst crypto = require('crypto');\nconst TZ = $env.PRACTICE_TZ || 'America/New_York';\nconst SALT = $env.PATIENT_HASH_SALT || '';\nconst envJson = (k) => { try { return JSON.parse($env[k] || '{}'); } catch (e) { return {}; } };\nconst sha256 = (s) => crypto.createHash('sha256').update(String(s)).digest('hex');\nconst patientRef = (id) => (id === null || id === undefined || id === '') ? null : sha256(String(id) + SALT);\nconst fmt = (d, opts) => new Intl.DateTimeFormat('en-US', Object.assign({ timeZone: TZ }, opts)).format(d);\nconst ordinal = (n) => n + ((n % 10 === 1 && n !== 11) ? 'st' : (n % 10 === 2 && n !== 12) ? 'nd' : (n % 10 === 3 && n !== 13) ? 'rd' : 'th');\nconst sayWhen = (iso) => { const d = new Date(iso); return fmt(d, { weekday: 'long' }) + ' the ' + ordinal(Number(fmt(d, { day: 'numeric' }))) + ' at ' + fmt(d, { hour: 'numeric', minute: '2-digit' }); };\nconst providerFirst = (id, fallbackName) => {\n  const names = envJson('NEXHEALTH_PROVIDER_NAMES');\n  if (id !== null && id !== undefined && names[String(id)]) return names[String(id)];\n  if (fallbackName) { const parts = String(fallbackName).replace(/^(dr\\.?|doctor)\\s+/i, '').trim().split(/\\s+/); return parts.length ? 'Dr. ' + parts[parts.length - 1] : null; }\n  return null;\n};\nconst e164 = (p) => { if (!p) return null; let d = String(p).replace(/[^\\d+]/g, ''); if (d.startsWith('+')) return d; d = d.replace(/\\D/g, ''); if (d.length === 10) return '+1' + d; if (d.length === 11 && d.startsWith('1')) return '+' + d; return d ? '+' + d : null; };\n// ---- end helpers ----\n\n// Input args: { patient_id, slot_id (from get_slots), appointment_type? }\nconst input = $('Sub-workflow trigger').first().json;\nconst args = input.args || {};\nconst parts = String(args.slot_id || '').split('|');\nconst start_time = parts[0] || null;\nif (!start_time || isNaN(new Date(start_time))) throw new Error('book_appointment requires a slot_id returned by get_slots');\nconst types = envJson('NEXHEALTH_APPT_TYPE_IDS');\nconst appointment_type_id = parts[3] || args.appointment_type_id || types[String(args.appointment_type || '').toLowerCase()] || null;\nconst appt = {\n  patient_id: Number(args.patient_id),\n  provider_id: Number(parts[1] || args.provider_id),\n  start_time,\n  note: 'Booked by AI receptionist',\n  referrer: 'ai-receptionist',\n};\nif (!appt.patient_id || !appt.provider_id) throw new Error('book_appointment requires patient_id and a slot that carries a provider');\nif (parts[2]) appt.operatory_id = Number(parts[2]);\nif (appointment_type_id) appt.appointment_type_id = Number(appointment_type_id);\nconst q = new URLSearchParams({ subdomain: $env.NEXHEALTH_SUBDOMAIN || '', location_id: String(input.location_id || ''), notify_patient: 'false' });\nreturn [{ json: { token: $json.token, url: 'https://nexhealth.info/appointments?' + q.toString(), body: { appt }, location: input.location } }];\n"
      },
      "id": "dc8aaa00-11b6-50d4-995c-f561f662bc30",
      "name": "Build request",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1100,
        300
      ]
    },
    {
      "parameters": {
        "method": "POST",
        "url": "={{ $json.url }}",
        "options": {
          "timeout": 15000
        },
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            {
              "name": "Accept",
              "value": "application/vnd.Nexhealth+json;version=2"
            },
            {
              "name": "Nex-Api-Version",
              "value": "v3.0.0"
            },
            {
              "name": "Authorization",
              "value": "=Bearer {{ $json.token }}"
            }
          ]
        },
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ JSON.stringify($json.body) }}"
      },
      "id": "adc6423a-e91b-53bb-b18c-11da0e5a7e44",
      "name": "NexHealth: POST /appointments",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        1320,
        300
      ],
      "onError": "continueErrorOutput"
    },
    {
      "parameters": {
        "jsCode": "// ---- shared helpers (duplicated in each workflow on purpose: n8n Code nodes cannot import) ----\nconst crypto = require('crypto');\nconst TZ = $env.PRACTICE_TZ || 'America/New_York';\nconst SALT = $env.PATIENT_HASH_SALT || '';\nconst envJson = (k) => { try { return JSON.parse($env[k] || '{}'); } catch (e) { return {}; } };\nconst sha256 = (s) => crypto.createHash('sha256').update(String(s)).digest('hex');\nconst patientRef = (id) => (id === null || id === undefined || id === '') ? null : sha256(String(id) + SALT);\nconst fmt = (d, opts) => new Intl.DateTimeFormat('en-US', Object.assign({ timeZone: TZ }, opts)).format(d);\nconst ordinal = (n) => n + ((n % 10 === 1 && n !== 11) ? 'st' : (n % 10 === 2 && n !== 12) ? 'nd' : (n % 10 === 3 && n !== 13) ? 'rd' : 'th');\nconst sayWhen = (iso) => { const d = new Date(iso); return fmt(d, { weekday: 'long' }) + ' the ' + ordinal(Number(fmt(d, { day: 'numeric' }))) + ' at ' + fmt(d, { hour: 'numeric', minute: '2-digit' }); };\nconst providerFirst = (id, fallbackName) => {\n  const names = envJson('NEXHEALTH_PROVIDER_NAMES');\n  if (id !== null && id !== undefined && names[String(id)]) return names[String(id)];\n  if (fallbackName) { const parts = String(fallbackName).replace(/^(dr\\.?|doctor)\\s+/i, '').trim().split(/\\s+/); return parts.length ? 'Dr. ' + parts[parts.length - 1] : null; }\n  return null;\n};\nconst e164 = (p) => { if (!p) return null; let d = String(p).replace(/[^\\d+]/g, ''); if (d.startsWith('+')) return d; d = d.replace(/\\D/g, ''); if (d.length === 10) return '+1' + d; if (d.length === 11 && d.startsWith('1')) return '+' + d; return d ? '+' + d : null; };\n// ---- end helpers ----\n\n// 201 response: { data: { appt: { id, start_time, provider_id, provider_name, ... } } }\nconst appt = ($json.data && $json.data.appt) || {};\nconst req = $('Build request').first().json;\nconst prov = providerFirst(appt.provider_id || req.body.appt.provider_id, appt.provider_name);\nconst when = sayWhen(appt.start_time || req.body.appt.start_time);\nreturn [{ json: { status: 'booked', appointment_id: String(appt.id || ''), say: 'You are booked for ' + when + (prov ? ' with ' + prov : '') + ' at our ' + (req.location || '') + ' location.' } }];\n"
      },
      "id": "94f2e91f-e6bc-5a05-bfc1-debfe366911e",
      "name": "Booked result",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1540,
        300
      ]
    }
  ],
  "connections": {
    "Prepare queue item": {
      "main": [
        [
          {
            "node": "Run 12 booking queue",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Run 12 booking queue": {
      "main": [
        [
          {
            "node": "Queued result",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Sub-workflow trigger": {
      "main": [
        [
          {
            "node": "Token cache",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Token cache": {
      "main": [
        [
          {
            "node": "Has cached token?",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Has cached token?": {
      "main": [
        [
          {
            "node": "Build request",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "NexHealth: authenticate",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "NexHealth: authenticate": {
      "main": [
        [
          {
            "node": "Cache token",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Prepare queue item",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Cache token": {
      "main": [
        [
          {
            "node": "Build request",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Build request": {
      "main": [
        [
          {
            "node": "NexHealth: POST /appointments",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "NexHealth: POST /appointments": {
      "main": [
        [
          {
            "node": "Booked result",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Prepare queue item",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "active": false,
  "settings": {
    "executionOrder": "v1",
    "saveManualExecutions": true,
    "saveDataErrorExecution": "all",
    "saveDataSuccessExecution": "all",
    "executionTimeout": 120
  },
  "pinData": {},
  "meta": {
    "templateCredsSetupCompleted": false,
    "instanceId": "dental-ai-receptionist"
  },
  "id": "dental04bookappt",
  "versionId": "e0b8667f-3945-55a2-9178-f12190196079"
}
