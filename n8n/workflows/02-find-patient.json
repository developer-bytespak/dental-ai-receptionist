{
  "name": "02 - Find patient",
  "nodes": [
    {
      "parameters": {
        "inputSource": "passthrough"
      },
      "id": "17e2ee6c-84e5-540d-b801-3a74b70cef39",
      "name": "Sub-workflow trigger",
      "type": "n8n-nodes-base.executeWorkflowTrigger",
      "typeVersion": 1.1,
      "position": [
        0,
        300
      ]
    },
    {
      "parameters": {
        "jsCode": "return [{ json: { status: 'error' } }];"
      },
      "id": "019437b6-e460-53e6-8ce8-2e8bcf4e54b4",
      "name": "PMS error result",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1320,
        600
      ]
    },
    {
      "parameters": {
        "jsCode": "// Returns a cached NexHealth bearer token when still valid (cached 50 min in workflow static data).\n// Static data only persists for production (activated) executions; manual test runs re-authenticate.\nconst sd = $getWorkflowStaticData('global');\nconst valid = sd.nexhealthToken && sd.nexhealthTokenExpires && Number(sd.nexhealthTokenExpires) > Date.now();\nreturn [{ json: { token: valid ? sd.nexhealthToken : null } }];\n"
      },
      "id": "850b1831-6b52-5347-84cc-c8fda62f6550",
      "name": "Token cache",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        220,
        300
      ]
    },
    {
      "parameters": {
        "conditions": {
          "options": {
            "caseSensitive": true,
            "leftValue": "",
            "typeValidation": "loose"
          },
          "conditions": [
            {
              "id": "b0ddd064-742f-54f6-83c0-6ddfb571687c",
              "leftValue": "={{ !!$json.token }}",
              "rightValue": "",
              "operator": {
                "type": "boolean",
                "operation": "true",
                "singleValue": true
              }
            }
          ],
          "combinator": "and"
        },
        "options": {}
      },
      "id": "7d6d35ab-b8f6-57a5-9867-d815e88c2781",
      "name": "Has cached token?",
      "type": "n8n-nodes-base.if",
      "typeVersion": 2,
      "position": [
        440,
        300
      ]
    },
    {
      "parameters": {
        "method": "POST",
        "url": "https://nexhealth.info/authenticates",
        "options": {
          "timeout": 15000
        },
        "authentication": "genericCredentialType",
        "genericAuthType": "httpHeaderAuth",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            {
              "name": "Accept",
              "value": "application/vnd.Nexhealth+json;version=2"
            },
            {
              "name": "Nex-Api-Version",
              "value": "v3.0.0"
            }
          ]
        }
      },
      "id": "823104ce-7bb0-5a45-86c7-30e62fef8c6e",
      "name": "NexHealth: authenticate",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        660,
        500
      ],
      "credentials": {
        "httpHeaderAuth": {
          "name": "NexHealth API"
        }
      },
      "onError": "continueErrorOutput",
      "notes": "Header Auth credential 'NexHealth API' supplies Authorization: <NexHealth API key>. Response data.token is a JWT used as Bearer on every other call.",
      "notesInFlow": true
    },
    {
      "parameters": {
        "jsCode": "// POST https://nexhealth.info/authenticates responds { code, data: { token } }.\nconst sd = $getWorkflowStaticData('global');\nconst token = $json.data && $json.data.token;\nif (!token) { throw new Error('NexHealth /authenticates returned no token'); }\nsd.nexhealthToken = token;\nsd.nexhealthTokenExpires = Date.now() + 50 * 60 * 1000;\nreturn [{ json: { token } }];\n"
      },
      "id": "14b00753-3263-58a2-b12f-1112a46b55ad",
      "name": "Cache token",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        880,
        500
      ]
    },
    {
      "parameters": {
        "jsCode": "// ---- shared helpers (duplicated in each workflow on purpose: n8n Code nodes cannot import) ----\nconst crypto = require('crypto');\nconst TZ = $env.PRACTICE_TZ || 'America/New_York';\nconst SALT = $env.PATIENT_HASH_SALT || '';\nconst envJson = (k) => { try { return JSON.parse($env[k] || '{}'); } catch (e) { return {}; } };\nconst sha256 = (s) => crypto.createHash('sha256').update(String(s)).digest('hex');\nconst patientRef = (id) => (id === null || id === undefined || id === '') ? null : sha256(String(id) + SALT);\nconst fmt = (d, opts) => new Intl.DateTimeFormat('en-US', Object.assign({ timeZone: TZ }, opts)).format(d);\nconst ordinal = (n) => n + ((n % 10 === 1 && n !== 11) ? 'st' : (n % 10 === 2 && n !== 12) ? 'nd' : (n % 10 === 3 && n !== 13) ? 'rd' : 'th');\nconst sayWhen = (iso) => { const d = new Date(iso); return fmt(d, { weekday: 'long' }) + ' the ' + ordinal(Number(fmt(d, { day: 'numeric' }))) + ' at ' + fmt(d, { hour: 'numeric', minute: '2-digit' }); };\nconst providerFirst = (id, fallbackName) => {\n  const names = envJson('NEXHEALTH_PROVIDER_NAMES');\n  if (id !== null && id !== undefined && names[String(id)]) return names[String(id)];\n  if (fallbackName) { const parts = String(fallbackName).replace(/^(dr\\.?|doctor)\\s+/i, '').trim().split(/\\s+/); return parts.length ? 'Dr. ' + parts[parts.length - 1] : null; }\n  return null;\n};\nconst e164 = (p) => { if (!p) return null; let d = String(p).replace(/[^\\d+]/g, ''); if (d.startsWith('+')) return d; d = d.replace(/\\D/g, ''); if (d.length === 10) return '+1' + d; if (d.length === 11 && d.startsWith('1')) return '+' + d; return d ? '+' + d : null; };\n// ---- end helpers ----\n\n// Input (from 01 Normalize request): { name, call_id, args: { name, date_of_birth }, location, location_id }\nconst input = $('Sub-workflow trigger').first().json;\nconst args = input.args || {};\nconst dobRaw = args.date_of_birth || args.dob || '';\nlet dob = null;\nif (dobRaw) {\n  if (/^\\d{4}-\\d{2}-\\d{2}$/.test(String(dobRaw).trim())) dob = String(dobRaw).trim();\n  else { const d = new Date(dobRaw); if (!isNaN(d)) dob = d.toISOString().slice(0, 10); }\n}\nconst q = new URLSearchParams();\nq.set('subdomain', $env.NEXHEALTH_SUBDOMAIN || '');\nq.set('location_id', String(input.location_id || ''));\nif (args.name) q.set('name', String(args.name).trim());\nif (dob) q.set('date_of_birth', dob);\nq.set('per_page', '25');\nreturn [{ json: { token: $json.token, url: 'https://nexhealth.info/patients?' + q.toString(), name: args.name || null, date_of_birth: dob } }];\n"
      },
      "id": "8ce0a976-5353-52b1-bafc-10b3dfe1efab",
      "name": "Build request",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1100,
        300
      ]
    },
    {
      "parameters": {
        "method": "GET",
        "url": "={{ $json.url }}",
        "options": {
          "timeout": 15000
        },
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            {
              "name": "Accept",
              "value": "application/vnd.Nexhealth+json;version=2"
            },
            {
              "name": "Nex-Api-Version",
              "value": "v3.0.0"
            },
            {
              "name": "Authorization",
              "value": "=Bearer {{ $json.token }}"
            }
          ]
        }
      },
      "id": "0cc4c9c5-e4ee-5b39-953d-446379eb4469",
      "name": "NexHealth: GET /patients",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        1320,
        300
      ],
      "onError": "continueErrorOutput"
    },
    {
      "parameters": {
        "jsCode": "// Exactly one active match (name + DOB) -> minimal fields only. Never phone, email or address.\nconst req = $('Build request').first().json;\nconst data = Array.isArray($json.data) ? $json.data : [];\nlet matches = data.filter(p => !p.inactive);\nif (req.date_of_birth) matches = matches.filter(p => p.bio && String(p.bio.date_of_birth || '').slice(0, 10) === req.date_of_birth);\nif (req.name && matches.length > 1) {\n  const n = String(req.name).trim().toLowerCase().replace(/\\s+/g, ' ');\n  const exact = matches.filter(p => String(p.name || ((p.first_name || '') + ' ' + (p.last_name || ''))).trim().toLowerCase().replace(/\\s+/g, ' ') === n);\n  if (exact.length === 1) matches = exact;\n}\nif (matches.length !== 1) return [{ json: { status: 'not_found' } }];\nconst p = matches[0];\nreturn [{ json: { patient_id: String(p.id), first_name: p.first_name || null } }];\n"
      },
      "id": "f5bb9e89-1f05-5e37-8276-f7292a6a9d3f",
      "name": "Pick single match",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1540,
        300
      ]
    }
  ],
  "connections": {
    "Sub-workflow trigger": {
      "main": [
        [
          {
            "node": "Token cache",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Token cache": {
      "main": [
        [
          {
            "node": "Has cached token?",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Has cached token?": {
      "main": [
        [
          {
            "node": "Build request",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "NexHealth: authenticate",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "NexHealth: authenticate": {
      "main": [
        [
          {
            "node": "Cache token",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "PMS error result",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Cache token": {
      "main": [
        [
          {
            "node": "Build request",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Build request": {
      "main": [
        [
          {
            "node": "NexHealth: GET /patients",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "NexHealth: GET /patients": {
      "main": [
        [
          {
            "node": "Pick single match",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "PMS error result",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "active": false,
  "settings": {
    "executionOrder": "v1",
    "saveManualExecutions": true,
    "saveDataErrorExecution": "all",
    "saveDataSuccessExecution": "all",
    "executionTimeout": 120
  },
  "pinData": {},
  "meta": {
    "templateCredsSetupCompleted": false,
    "instanceId": "dental-ai-receptionist"
  },
  "id": "dental02findpat0",
  "versionId": "cdbfe07b-2b99-5036-a330-6491268da224"
}
